services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: supervacanta
      POSTGRES_PASSWORD: supervacanta
      POSTGRES_DB: supervacanta
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
