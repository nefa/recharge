services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: recharge
      POSTGRES_PASSWORD: recharge
      POSTGRES_DB: recharge
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
